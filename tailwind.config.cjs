const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				DotMatrix: ['DotMatrix', 'sans-serif']
			}
		}
	},

	plugins: []
};

module.exports = config;
